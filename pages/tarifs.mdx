import DocsLayout from '@/components/DocsLayout';
import { Badge } from '@chakra-ui/core';

export default (props) => <DocsLayout {...props} />;

<Badge fontSize="0.8em">beta</Badge> 

# Tarifs

Pour sa version beta, obtenez un nombre illimité de commentaires sur autant
de sites que vous les souhaitez pour 9.99€/mois

Contactez nous à cette adresse support@reliable.com pour toute ques-tions.

