import DocsLayout from '@/components/DocsLayout';
import { Badge } from '@chakra-ui/core';

export default (props) => <DocsLayout {...props} />;

# Tarifs

Pour sa version alpha, obtenez un nombre illimité de commentaires sur autant
de sites que souhaité pour 9.99€/mois. Un essai gratuit de 14 jours
est offert à tout nouvel utilisateur. Une validation par carte bancaire est
nécessaire afin de contrer tout abus de cette offre.

Contactez nous à cette adresse heysen.florian@gmail.com pour toute questions.